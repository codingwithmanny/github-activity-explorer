// MAIN APP ENV VARS
/**
 * Main api url for github api
 * @constant url
 */
window.API_URL = '';

/**
 * Main GitHub Commit SHA
 * @constant key
 */
window.GITHUB_COMMIT = '';

/**
 * Main GitHub Repository URL
 * @constant key
 */
window.GITHUB_REPO_URL = '';

/**
 * Main GitHub Repository URL
 * @constant key
 */
window.GITHUB_USER = '';

/**
 * Main Netlify Deploy ID
 * @constant key
 */
window.DEPLOY_ID = '';

/**
 * Main Netlify Deploy URL
 * @constant key
 */
window.DEPLOY_URL = '';
