// MAIN APP ENV VARS
/**
 * Main api url for github api
 * @constant url
 */
window.API_URL = '';

/**
 * Main GitHub Commit SHA
 * @constant key
 */
window.GITHUB_COMMIT = 'LOCAL_DEVELOPMENT';

/**
 * Main GitHub Repository URL
 * @constant key
 */
window.GITHUB_REPO_URL =
  'https://github.com/codingwithmanny/github-activity-explorer';

/**
 * Main GitHub Repository URL
 * @constant key
 */
window.GITHUB_USER = 'codingwithmanny';

/**
 * Main Netlify Deploy ID
 * @constant key
 */
window.DEPLOY_ID = 'LOCAL_DEPLOYMENT';

/**
 * Main Netlify Deploy URL
 * @constant key
 */
window.DEPLOY_URL = '/';
